{
  "actionButtons": {
    "defaultColor": "white",
    "loadNpmCommands": false,
    "reloadButton": "$(sync)",
    "commands": [
      {
        "name": "$(rocket) Start Dashboard",
        "color": "green",
        "singleInstance": true,
        "cwd": "${workspaceFolder}",
        "command": "pnpm dev --hostname 0.0.0.0"
      },
      {
        "name": "$(debug-stop) Stop Dashboard",
        "color": "#aa0202",
        "command": "pkill -f 'next dev' || true"
      }
    ]
  }
}
